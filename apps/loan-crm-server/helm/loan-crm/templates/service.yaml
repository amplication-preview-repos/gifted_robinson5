apiVersion: v1
kind: Service
metadata:
  name: {{ include "loan-crm.fullname" . }}
  labels:
    {{- include "loan-crm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loan-crm.selectorLabels" . | nindent 4 }}
